@model gpos.Models.ViewModels.FuelSetupPageViewModel

<div class="page-header">
    <div class="page-heading">
        <nav aria-label="breadcrumb">
            <ol class="breadcrumb">
                <li class="breadcrumb-item">GPOS</li>
                <li class="breadcrumb-item">Setup</li>
                <li class="breadcrumb-item">Fuel</li>
                <li class="breadcrumb-item active" aria-current="page">Pumps</li>
            </ol>
        </nav>
        <h1>Pumps</h1>
        <p class="page-subtitle">Connect each pump to the tank it dispenses from.</p>
    </div>
</div>

<section class="content-card setup-crud-card setup-table-card">
    @if (TempData["FuelSetupFeedback"] is string feedback)
    {
        <div class="alert alert-info mb-0" role="alert">@feedback</div>
    }
    <div class="table-actions fuel-toolbar">
        <form asp-action="Pumps" method="get" class="setup-search-form fuel-toolbar-search">
            <input class="form-control" name="search" value="@Model.Search" placeholder="Search by name or tank" />
        </form>
        <button class="btn btn-primary" type="button" data-bs-toggle="modal" data-bs-target="#pumpModal">Add Pump</button>
    </div>
    <div class="setup-table-scroll">
        <table class="table app-table table-sm align-middle mb-0">
            <thead>
                <tr>
                    <th>No</th>
                    <th>ID</th>
                    <th>Name</th>
                    <th>Tank</th>
                    <th>Fuel</th>
                    <th>Action</th>
                </tr>
            </thead>
            <tbody>
                @if (Model.Pumps.Count == 0)
                {
                    <tr><td class="text-center text-muted fw-bold" colspan="6">No records yet.</td></tr>
                }
                @for (var i = 0; i < Model.Pumps.Count; i++)
                {
                    <tr>
                        <td>@(i + 1)</td>
                        <td>@Model.Pumps[i].Id</td>
                        <td>@Model.Pumps[i].Name</td>
                        <td>@Model.Pumps[i].Tank?.TankNo</td>
                        <td>@Model.Pumps[i].Tank?.Fuel?.Name</td>
                        <td>
                            <div class="setup-row-actions">
                                <a class="btn btn-sm btn-outline-primary" asp-action="Pumps" asp-route-editId="@Model.Pumps[i].Id" asp-route-search="@Model.Search">Edit</a>
                                <form asp-action="DeletePump" method="post" onsubmit="return confirm('Delete this pump?');">
                                    @Html.AntiForgeryToken()
                                    <input name="id" type="hidden" value="@Model.Pumps[i].Id" />
                                    <input name="search" type="hidden" value="@Model.Search" />
                                    <button class="btn btn-sm btn-outline-danger" type="submit">Delete</button>
                                </form>
                            </div>
                        </td>
                    </tr>
                }
            </tbody>
        </table>
    </div>
</section>

<div class="modal fade" id="pumpModal" tabindex="-1" aria-labelledby="pumpModalLabel" aria-hidden="true">
    <div class="modal-dialog modal-dialog-centered">
        <div class="modal-content">
            <form asp-action="SavePump" method="post">
                @Html.AntiForgeryToken()
                <input asp-for="PumpForm.Id" type="hidden" />
                <input name="search" type="hidden" value="@Model.Search" />
                <div class="modal-header">
                    <h2 class="modal-title fs-5" id="pumpModalLabel">Add Pump</h2>
                    <button class="btn-close" type="button" data-bs-dismiss="modal" aria-label="Close"></button>
                </div>
                <div class="modal-body">
                    <div class="mb-3">
                        <label asp-for="PumpForm.Name" class="form-label">Name</label>
                        <input asp-for="PumpForm.Name" class="form-control" placeholder="Pump A" />
                        <span asp-validation-for="PumpForm.Name" class="text-danger"></span>
                    </div>
                    <div class="mb-3">
                        <label asp-for="PumpForm.TankId" class="form-label">Tank</label>
                        <select asp-for="PumpForm.TankId" asp-items="Model.TankOptions" class="form-select">
                            <option value="0">Select tank</option>
                        </select>
                        <span asp-validation-for="PumpForm.TankId" class="text-danger"></span>
                    </div>
                </div>
                <div class="modal-footer">
                    <button class="btn btn-outline-secondary" type="button" data-bs-dismiss="modal">Cancel</button>
                    <button class="btn btn-primary" type="submit">Save</button>
                </div>
            </form>
        </div>
    </div>
</div>

@section Scripts {
    <partial name="_ValidationScriptsPartial" />
    <script>
        document.addEventListener("DOMContentLoaded", () => {
            const modalId = "@Model.ActiveModalId";
            const modalElement = modalId ? document.getElementById(modalId) : null;

            if (modalElement && window.bootstrap) {
                bootstrap.Modal.getOrCreateInstance(modalElement).show();
            }
        });
    </script>
}
