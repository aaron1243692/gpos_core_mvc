@model gpos.Models.SalesmanLoginViewModel

@{
    Layout = "_LandingLayout";
    ViewData["Title"] = "Salesman Login";
}

<main class="salesman-login-wrap">
    <section class="salesman-login-card" aria-labelledby="salesmanLoginTitle">
        <div class="home-brand-mark" aria-hidden="true">S</div>
        <span class="pos-eyebrow">Salesman POS</span>
        <h1 id="salesmanLoginTitle">Sign in to POS</h1>
        <p>Use your employee account to open the point of sale workspace.</p>

        <form asp-controller="SalesmanAuth" asp-action="Login" method="post" class="login-form" novalidate>
            @Html.AntiForgeryToken()

            <div class="mb-3">
                <label asp-for="Username" class="form-label">Email or Username</label>
                <input asp-for="Username" class="form-control login-identity-input" autocomplete="username" autofocus />
            </div>

            <div class="mb-3">
                <label asp-for="Password" class="form-label"></label>
                <input asp-for="Password" class="form-control login-password-input" autocomplete="current-password" />
                <div asp-validation-summary="All" class="salesman-login-error login-feedback"></div>
            </div>

            <button class="btn btn-primary w-100" type="submit">Sign In</button>
        </form>
    </section>
</main>

@section Scripts {
    <partial name="_ValidationScriptsPartial" />
}
