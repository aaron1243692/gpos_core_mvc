@model gpos.Models.ViewModels.SetupModulesPageViewModel

<div class="page-header">
    <div class="page-heading">
        <nav aria-label="breadcrumb">
            <ol class="breadcrumb">
                <li class="breadcrumb-item">GPOS</li>
                <li class="breadcrumb-item">Setup</li>
                <li class="breadcrumb-item">Fuel</li>
                <li class="breadcrumb-item active">Dispenser Meter Readings</li>
            </ol>
        </nav>
        <h1>Dispenser Meter Readings</h1>
        <p class="page-subtitle">Record opening and closing readings by nozzle, dispenser, tank, and fuel.</p>
    </div>
</div>

<section class="content-card setup-crud-card setup-table-card">
    <div class="table-actions fuel-toolbar">
        <form asp-action="PumpMeterReadings" method="get" class="setup-search-form fuel-toolbar-search">
            <input class="form-control" name="search" value="@Model.Search" placeholder="Search readings" />
        </form>
        <button class="btn btn-primary" type="button" data-bs-toggle="modal" data-bs-target="#pumpMeterReadingModal">Add Reading</button>
    </div>
    <div class="setup-table-scroll">
        <table class="table app-table table-sm align-middle admin-table pump-meter-readings-table mb-0">
            <colgroup>
                <col class="col-no" />
                <col class="col-id" />
                <col class="col-name" />
                <col class="col-nozzle" />
                <col class="col-pump" />
                <col class="col-tank" />
                <col class="col-fuel" />
                <col class="col-meter" />
                <col class="col-meter" />
                <col class="col-meter" />
                <col class="col-remarks" />
                <col class="col-date" />
                <col class="col-action" />
            </colgroup>
            <thead>
                <tr>
                    <th>No</th>
                    <th>ID</th>
                    <th>Name</th>
                    <th>Nozzle</th>
                    <th>Dispenser</th>
                    <th>Tank</th>
                    <th>Fuel</th>
                    <th>Opening Meter</th>
                    <th>Closing Meter</th>
                    <th>Liters Sold</th>
                    <th>Remarks</th>
                    <th>Reading Date</th>
                    <th>Action</th>
                </tr>
            </thead>
            <tbody>
                @if (Model.PumpMeterReadings.Count == 0)
                {
                    <tr><td class="text-center text-muted fw-bold" colspan="13">No dispenser meter readings yet.</td></tr>
                }
                @for (var i = 0; i < Model.PumpMeterReadings.Count; i++)
                {
                    var readingTank = Model.PumpMeterReadings[i].Nozzle?.Tank ?? Model.PumpMeterReadings[i].Nozzle?.Pump?.Tank ?? Model.PumpMeterReadings[i].Pump?.Tank;
                    var readingFuel = readingTank?.Fuel;
                    <tr>
                        <td>@(i + 1)</td>
                        <td>@Model.PumpMeterReadings[i].Id</td>
                        <td class="cell-text">@Model.PumpMeterReadings[i].Name</td>
                        <td class="cell-text">@(Model.PumpMeterReadings[i].Nozzle?.NozzleNo ?? "-")</td>
                        <td class="cell-text">@(Model.PumpMeterReadings[i].Nozzle?.Pump?.Name ?? Model.PumpMeterReadings[i].Pump?.Name ?? "-")</td>
                        <td class="cell-text">@(readingTank?.TankNo ?? "-")</td>
                        <td class="cell-text">@(readingFuel?.Name ?? "-")</td>
                        <td class="cell-number">@Model.PumpMeterReadings[i].OpeningMeter.ToString("N2")</td>
                        <td class="cell-number">@(Model.PumpMeterReadings[i].ClosingMeter?.ToString("N2") ?? "-")</td>
                        <td class="cell-number">@(Model.PumpMeterReadings[i].LitersSold?.ToString("N2") ?? "-")</td>
                        <td class="cell-remarks" title="@Model.PumpMeterReadings[i].Remarks">@(string.IsNullOrWhiteSpace(Model.PumpMeterReadings[i].Remarks) ? "-" : Model.PumpMeterReadings[i].Remarks)</td>
                        <td class="cell-date">@Model.PumpMeterReadings[i].ReadingDate.ToString("yyyy-MM-dd")</td>
                        <td><a class="btn btn-sm btn-outline-primary" asp-action="PumpMeterReadings" asp-route-search="@Model.Search" asp-route-editId="@Model.PumpMeterReadings[i].Id">Edit</a></td>
                    </tr>
                }
            </tbody>
        </table>
    </div>
</section>

<div class="modal fade" id="pumpMeterReadingModal" tabindex="-1" aria-hidden="true">
    <div class="modal-dialog modal-dialog-centered">
        <div class="modal-content">
            <form asp-action="SavePumpMeterReading" method="post">
                @Html.AntiForgeryToken()
                <input asp-for="PumpMeterReadingForm.Id" type="hidden" />
                <input name="search" type="hidden" value="@Model.Search" />
                <div class="modal-header">
                    <h2 class="modal-title fs-5">Dispenser Meter Reading</h2>
                    <button class="btn-close" type="button" data-bs-dismiss="modal" aria-label="Close"></button>
                </div>
                <div class="modal-body">
                    <div class="mb-3">
                        <label asp-for="PumpMeterReadingForm.Name" class="form-label">Name</label>
                        <input asp-for="PumpMeterReadingForm.Name" class="form-control" placeholder="Enter reading name" />
                        <span asp-validation-for="PumpMeterReadingForm.Name" class="text-danger"></span>
                    </div>
                    <div class="mb-3">
                        <label asp-for="PumpMeterReadingForm.NozzleId" class="form-label">Nozzle</label>
                        <select asp-for="PumpMeterReadingForm.NozzleId" class="form-select" data-nozzle-reading-source data-selected-nozzle="@Model.PumpMeterReadingForm.NozzleId">
                            <option value="0" data-pump="" data-tank="" data-fuel="">Select nozzle</option>
                            @for (var i = 0; i < Model.Nozzles.Count; i++)
                            {
                                var nozzleTank = Model.Nozzles[i].Tank ?? Model.Nozzles[i].Pump?.Tank;
                                var nozzleFuel = nozzleTank?.Fuel;
                                <option value="@Model.Nozzles[i].Id" data-pump="@(Model.Nozzles[i].Pump?.Name ?? string.Empty)" data-tank="@(nozzleTank?.TankNo ?? string.Empty)" data-fuel="@(nozzleFuel?.Name ?? string.Empty)">@(Model.Nozzles[i].NozzleNo) - @(Model.Nozzles[i].Pump?.Name ?? "-") - @(nozzleTank?.TankNo ?? "-") - @(nozzleFuel?.Name ?? "-")</option>
                            }
                        </select>
                        <span asp-validation-for="PumpMeterReadingForm.NozzleId" class="text-danger"></span>
                    </div>
                    <div class="row g-2">
                        <div class="col-md-4 mb-3">
                            <label class="form-label">Dispenser</label>
                            <input class="form-control" type="text" data-derived-pump readonly />
                        </div>
                        <div class="col-md-4 mb-3">
                            <label class="form-label">Tank</label>
                            <input class="form-control" type="text" data-derived-tank readonly />
                        </div>
                        <div class="col-md-4 mb-3">
                            <label class="form-label">Fuel</label>
                            <input class="form-control" type="text" data-derived-fuel readonly />
                        </div>
                    </div>
                    <div class="row g-2">
                        <div class="col-md-6 mb-3">
                            <label asp-for="PumpMeterReadingForm.OpeningMeter" class="form-label">Opening Meter</label>
                            <input asp-for="PumpMeterReadingForm.OpeningMeter" class="form-control" type="number" step="0.01" min="0" />
                            <span asp-validation-for="PumpMeterReadingForm.OpeningMeter" class="text-danger"></span>
                        </div>
                        <div class="col-md-6 mb-3">
                            <label asp-for="PumpMeterReadingForm.ClosingMeter" class="form-label">Closing Meter</label>
                            <input asp-for="PumpMeterReadingForm.ClosingMeter" class="form-control" type="number" step="0.01" min="0" />
                            <span asp-validation-for="PumpMeterReadingForm.ClosingMeter" class="text-danger"></span>
                        </div>
                    </div>
                    <div class="mb-3">
                        <label asp-for="PumpMeterReadingForm.Remarks" class="form-label">Remarks</label>
                        <textarea asp-for="PumpMeterReadingForm.Remarks" class="form-control" rows="3" placeholder="Enter remarks"></textarea>
                        <span asp-validation-for="PumpMeterReadingForm.Remarks" class="text-danger"></span>
                    </div>
                    <div class="mb-3">
                        <label asp-for="PumpMeterReadingForm.ReadingDate" class="form-label">Reading Date</label>
                        <input asp-for="PumpMeterReadingForm.ReadingDate" class="form-control" type="date" />
                        <span asp-validation-for="PumpMeterReadingForm.ReadingDate" class="text-danger"></span>
                    </div>
                    <input asp-for="PumpMeterReadingForm.Status" type="hidden" />
                </div>
                <div class="modal-footer">
                    <button class="btn btn-outline-secondary" type="button" data-bs-dismiss="modal">Cancel</button>
                    <button class="btn btn-primary" type="submit">Save</button>
                </div>
            </form>
        </div>
    </div>
</div>

@section Scripts {
    <partial name="_ValidationScriptsPartial" />
    <script>
        document.addEventListener("DOMContentLoaded", () => {
            const modalId = "@Model.ActiveModalId";
            const modalElement = modalId ? document.getElementById(modalId) : null;

            if (modalElement && window.bootstrap) {
                bootstrap.Modal.getOrCreateInstance(modalElement).show();
            }

            const nozzleSelect = document.querySelector("[data-nozzle-reading-source]");
            const pumpField = document.querySelector("[data-derived-pump]");
            const tankField = document.querySelector("[data-derived-tank]");
            const fuelField = document.querySelector("[data-derived-fuel]");

            if (!nozzleSelect || !pumpField || !tankField || !fuelField) {
                return;
            }

            const selectedNozzle = nozzleSelect.getAttribute("data-selected-nozzle");
            if (selectedNozzle) {
                nozzleSelect.value = selectedNozzle;
            }

            const updateDerivedFields = () => {
                const option = nozzleSelect.selectedOptions.length > 0 ? nozzleSelect.selectedOptions[0] : null;
                pumpField.value = option?.getAttribute("data-pump") || "";
                tankField.value = option?.getAttribute("data-tank") || "";
                fuelField.value = option?.getAttribute("data-fuel") || "";
            };

            nozzleSelect.addEventListener("change", updateDerivedFields);
            updateDerivedFields();
        });
    </script>
}
